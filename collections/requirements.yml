---
# Ansible Collections required for Red Hat Satellite 6.17 Installation
# These collections must be available in AAP

collections:
  - name: redhat.rhel_system_roles
    version: ">=1.20.0"
    
  - name: redhat.satellite_operations
    version: ">=2.0.0"
    
  - name: redhat.satellite
    version: ">=3.0.0"
    
  - name: ansible.posix
    version: ">=1.5.0"
    
  - name: community.general
    version: ">=6.0.0"
    
  # Optional - only needed if using Venafi for certificate generation
  # - name: venafi.machine_identity
  #   version: ">=0.9.0"

